# 🎯 快速参考卡

## 一句话使用

```bash
./start.sh  # 启动主菜单，跟着提示走
```

---

## 完整流程（3步）

### 1️⃣ 配置你的小说
```bash
python3 configure_novel.py
```
回答7组问题，系统会保存配置。

### 2️⃣ 生成小说
```bash
./run_novel.sh
```
坐等 AI 创作完成。

### 3️⃣ 查看结果
```bash
ls manuscript/你的小说名/
cat manuscript/你的小说名/chapter_001.md
```

---

## 核心文件

| 文件 | 作用 |
|------|------|
| `configure_novel.py` | 配置向导 |
| `run_novel.sh` | 运行生成器 |
| `start.sh` | 主菜单（推荐） |
| `bible/novel_config_latest.yaml` | 当前配置 |
| `manuscript/` | 生成的章节 |
| `bible/*_world_state.json` | 世界状态 |

---

## 让小说不同的关键参数

### 在配置时选择：

**随机性等级**（影响最大）
- 低 → 严谨可控
- 中 → 平衡（推荐）✅
- 高 → 脑洞大开

**伏笔策略**
- 保守 → 只用你的设定
- 适中 → AI 适当添加 ✅
- 激进 → AI 自由发挥

**角色自主性**
- 严格 → 按设定行动
- 适中 → 合理自主 ✅
- 自由 → 可能反常

### 手动调整（高级）

编辑 `bible/novel_config_latest.yaml`：

```yaml
generation:
  temperature: 0.7      # 0.2-1.2，越高越随机
  writer_temp: 0.9      # 写作温度
  seed: null            # null = 每次不同，123 = 固定
```

---

## 验证差异性

### 实验：同一配置，生成2次

```bash
# 第一次
./run_novel.sh
mv manuscript/你的小说名 manuscript/版本1

# 第二次
./run_novel.sh
mv manuscript/你的小说名 manuscript/版本2

# 对比
diff manuscript/版本1/chapter_001.md manuscript/版本2/chapter_001.md
```

你会发现内容完全不同！

---

## 常用命令速查

```bash
# 重新配置
python3 configure_novel.py

# 生成1章
./run_novel.sh

# 查看配置
cat bible/novel_config_latest.yaml

# 编辑配置
nano bible/novel_config_latest.yaml

# 查看生成的小说
ls -R manuscript/

# 主菜单（最简单）
./start.sh
```

---

## 故障排除

### 问题：未找到配置文件
```bash
# 解决：运行配置向导
python3 configure_novel.py
```

### 问题：API Key错误
```bash
# 解决：设置环境变量
nano .env
# 添加：ANTHROPIC_API_KEY=sk-ant-xxxxx
```

### 问题：生成太慢
```bash
# 解决：减少章节数
# 编辑配置，改为 target_chapters: 1
nano bible/novel_config_latest.yaml
```

---

## 模型映射（已配置好）

- **Planner**: Claude Sonnet 4.5 (逻辑推演)
- **Writer**: Claude Opus 4.5 (文学创作)
- **Critic**: Claude Sonnet 4.5 (质量把关)
- **Memory**: Claude Sonnet 4.5 (状态管理)

**温度自动调整**：
- 根据你选择的"随机性等级"自动设置
- 无需手动配置

---

## 🎉 开始创作！

```bash
./start.sh
```

祝你写出精彩的小说！📚✨
